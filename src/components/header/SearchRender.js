/* import React from 'react'
import { useParams } from 'react-router-dom'
import { PRODUCTDATA } from '../../data/Productdata'


const SearchRender = () => {
 let {slug} = useParams();
 //let products = PRODUCTDATA.filter(item => item.productname == slug);
    return (
        <div>
        A boy {slug}
        </div>
    )
}

export default SearchRender */